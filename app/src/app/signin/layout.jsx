export const metadata = {
  title: "Sign in",
  description: "Sign in to your findyourcofounder account.",
  alternates: { canonical: "/signin" },
  robots: { index: false, follow: true },
};

export default function SignInLayout({ children }) {
  return children;
}
